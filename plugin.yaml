name: a0_worktree
title: A0 Worktree
description: Give the agent an isolated git worktree + branch of a repo as its own project, so it can work safely off your main checkout. Also the worktree backend that A0 Swarm's isolated mode delegates to.
version: 1.1.0
settings_sections:
  - agent
per_project_config: false
per_agent_config: false
always_enabled: false
